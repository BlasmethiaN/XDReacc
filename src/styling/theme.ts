import { generate } from '@ant-design/colors'

const theme = {
  bgColor: { dark: '#181818', light: '#ff645b' },
  palette: { dark: generate('#181818'), light: generate('#ff645b') },
}
export default theme
